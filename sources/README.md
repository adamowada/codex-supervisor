# Sources

This directory is reserved for local source-study clones.

Tracked product material lives in the repository root, `src/`, `scripts/`, `tests/`, `plans/`,
`.agents/`, and `insights/`. Source-study clones are local inspection caches.

Any copied or adapted external material receives license review, attribution, and a decision record
when it enters the tracked product.
