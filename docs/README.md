# Docs

Use this directory for extended implementation notes, API references, and generated design reports
that are too detailed for the locked top-level source-of-truth documents.

Do not use this directory as a substitute for planning SQLite. Operational plans belong in
`plans/planning.sqlite3`.
